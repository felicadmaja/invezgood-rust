[package]
name = "user"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3"
scylla = "1.5"
tokio = { version = "1", features = ["sync"] }
tonic = "0.12"
prost = "0.13"
uuid = { version = "1", features = ["v4"] }

[build-dependencies]
tonic-build = "0.12"
