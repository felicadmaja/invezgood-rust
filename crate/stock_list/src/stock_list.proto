syntax = "proto3";

package stock_list;

service StockList {
  rpc List(ListRequest) returns (ListResponse);
}

message ListRequest {
  uint32 limit = 1;
}

message StockItem {
  string symbol = 1;
  string name = 2;
}

message ListResponse {
  repeated StockItem items = 1;
}
