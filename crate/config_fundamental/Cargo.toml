[package]
name = "config_fundamental"
version = "0.1.0"
edition = "2021"

[dependencies]
scylla = { version = "1.5", features = ["chrono-04"] }
tonic = "0.12"
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
