[package]
name = "portofolio_equity"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3"
prost = "0.13"
scylla = "1.5"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
tonic = "0.12"
user = { path = "../user" }

[build-dependencies]
tonic-build = "0.12"
