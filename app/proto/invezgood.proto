syntax = "proto3";

package invezgood;

service Invezgood {
  // Health check: echo message dari request sebagai "pong: {message}".
  rpc Ping(PingRequest) returns (PingResponse);
}

message PingRequest {
  string message = 1;
}

message PingResponse {
  string message = 1;
}
